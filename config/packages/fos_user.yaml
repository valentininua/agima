fos_user:
    db_driver: orm # other valid values are 'mongodb' and 'couchdb'
    firewall_name: main
    user_class: App\Entity\User
    from_email:
        address: "admin@valentin"
        sender_name: "admin@valentin"
    registration:
        confirmation:
            enabled: false
